# BetterMailTo

An example of how mailto links should be done, with React.

## Run the project

To run the project and see the example:

`bun dev` or `pnpm run dev` or `npm run dev`

Open `http://localhost:5173/` on your browser.
